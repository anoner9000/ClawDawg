"""
ops.governance — single-source governance library.

Rules:
- Pure functions only (no network, no filesystem side effects).
- Schemas + policy contracts are inputs.
- Callers (UI/agents/runtime) must be thin wrappers.
"""
